# AI Interview Coach - Frontend 프로젝트 분석

## 📋 프로젝트 개요

**AI Interview Coach**는 실시간 화상 면접 시뮬레이션과 AI 기반 피드백을 제공하는 웹 애플리케이션입니다.

- **프로젝트명**: frontend-repo
- **기술 스택**: React 19 + TypeScript + Vite
- **주요 기능**: 화상 면접, AI 질문 생성, 실시간 피드백, 결과 분석
- **아키텍처**: SPA (Single Page Application)

## 🏗️ 전체 아키텍처

### 핵심 아키텍처 패턴
```
┌─────────────────┐    ┌──────────────┐    ┌─────────────────┐
│   사용자 인터페이스   │◄──►│   상태 관리    │◄──►│   백엔드 연동     │
│  (React Pages)  │    │  (Zustand)   │    │ (API + WebSocket)│
└─────────────────┘    └──────────────┘    └─────────────────┘
         ▲                       ▲                       ▲
         │                       │                       │
┌─────────────────┐    ┌──────────────┐    ┌─────────────────┐
│   화상 통신      │    │   실시간 통신   │    │   미디어 처리     │
│  (OpenVidu)     │    │  (WebSocket)   │    │   (MediaRecorder)│
└─────────────────┘    └──────────────┘    └─────────────────┘
```

### 데이터 플로우
1. **사용자 입력** → 상태 관리 스토어
2. **API 호출** → 백엔드 서버
3. **실시간 데이터** → WebSocket → 콜백 함수
4. **미디어 스트림** → OpenVidu → 화면 렌더링

## 📱 페이지별 기능 분석

### 🏠 LandingPage
**위치**: `src/pages/Landing/LandingPage.tsx`
```tsx
// 랜딩 페이지 - 서비스 소개 및 시작점
- Hero 섹션: 메인 타이틀과 시작 버튼
- Features 섹션: 주요 기능 소개 (AI 분석, 실시간 피드백 등)
- Process 섹션: 면접 진행 과정 안내
- 반응형 디자인: Tailwind CSS 활용
```

### 🔐 인증 시스템
**위치**: `src/pages/Auth/`
```tsx
// LoginPage: 로그인 처리
- 일반 로그인 / 소셜 로그인 지원
- Form validation 및 에러 처리
- 로그인 성공 시 토큰 저장 및 리다이렉트

// RegisterPage: 회원가입 처리  
- 다단계 회원가입 폼
- 이메일 인증 프로세스
- 실시간 유효성 검사

// ForgotPasswordPage: 비밀번호 찾기
- 3단계 프로세스: 이메일 입력 → 인증번호 확인 → 새 비밀번호 설정
- 시간 제한 인증번호 시스템 (3분)
```

### 🎯 면접 설정
**위치**: `src/pages/InterviewSetup/`
```tsx
// InterviewSetupPage: 면접 준비
- Step1: 면접 유형 선택 (직무/인성/PT)
- Step2: 질문 개수 설정 (3/5개)
- Step3: 문서 업로드 (이력서/포트폴리오/발표자료)
- Step4: 최종 확인 및 면접 시작

// 주요 기능:
- 파일 드래그&드롭 업로드
- 기존 파일 선택 기능
- 실시간 폼 검증
- 회사명/직무명 표시 ({company} / {job}.pdf)
```

### 📚 면접 가이드
**위치**: `src/pages/InterviewGuide/`
```tsx
// InterviewGuideContent: 면접 전 안내
- 면접 안내사항 (복장, 환경, 답변 요령)
- 장비 확인 (카메라/마이크 테스트)
- 실시간 오디오 비주얼라이저
- 면접 시작 버튼 (장비 확인 완료 후 활성화)

// 주요 기능:
- MediaStream API를 통한 실시간 카메라/마이크 테스트
- 권한 체크 및 에러 처리
- 3초 카운트다운 후 세션 시작
```

### ⏳ 로딩 페이지
**위치**: `src/pages/InterviewLoading/`
```tsx
// InterviewLoadingPage: 면접 준비 대기
- 3단계 프로세스: prewait(5s) → polling(ready) → countdown(5s)
- AI 질문 생성 진행 상황 표시
- 면접 팁 자동 슬라이드
- SessionStorage 기반 질문 데이터 폴링

// 핵심 로직:
- 최소 5초 대기 보장
- 질문 생성 완료까지 최대 2분 폴링
- 타입별 쿼리 파라미터 보장 (?type=tech/behavioral/presentation)
```

### 🎥 면접 세션
**위치**: `src/pages/InterviewSession/`
```tsx
// InterviewSession: 핵심 면접 진행
- 질문 표시 및 답변 녹화
- 실시간 타이머 (60초 답변 시간)
- OpenVidu 화상 통신
- WebSocket 기반 실시간 피드백 수집

// 상태 관리:
- useReducer를 통한 복잡한 면접 상태 관리
- 질문 진행: loading → preparing → answering → complete
- 세트별 질문 관리 (본질문 + 꼬리질문 2개)
- 재시도 모드 지원

// 주요 기능:
- 자동 질문 세트 생성
- 답변 완료 시 자동 피드백 수집
- 세트 완료 시 즉시 피드백 페이지 이동
- PT 면접 별도 레이아웃
```

### 📊 결과 및 피드백
**위치**: `src/pages/ResultDetail/`, `src/pages/StepByStepFeedbackPage/`
```tsx
// StepByStepFeedbackPage: 단계별 피드백
- 질문별 상세 피드백 표시
- 점수 및 개선점 제공
- 다음 질문으로 넘어가기
- 재시도 기능

// ResultDetail: 전체 결과 분석
- 감정 분석 타임라인 그래프
- 전체 면접 통계
- 강점/약점 분석
- 추천 학습 자료
```

### 🔄 완료 화면
**위치**: `src/pages/InterviewSession/components/CompletionScreen.tsx`
```tsx
// CompletionScreen: 면접 완료 처리
- WebSocket 기반 실시간 분석 진행률 표시
- PT/일반면접 구분 처리
- 자동 결과 페이지 이동
- 분석 완료 대기 상태 관리

// 핵심 로직:
- answerAttemptId 수집 및 관리
- PT의 경우 attemptUuid 기반 처리
- 모든 분석 완료 시 자동 이동
```

## 🔄 데이터 플로우 및 상태 관리

### Zustand 스토어 구조

#### 1. AuthStore (`src/shared/store/authStore.ts`)
```tsx
interface AuthState {
  // 인증 상태
  isLoggedIn: boolean;
  userId: string | null;
  username: string | null;
  accessToken: string | null;
  
  // 회원가입/비밀번호 찾기 상태
  register: RegisterState;
  forgotPassword: ForgotPasswordState;
  
  // 액션들
  login, logout, refreshAccessToken, initializeAuth;
}
```

#### 2. WebSocketStore (`src/shared/store/websocketStore.ts`)
```tsx
interface WebSocketState {
  // 연결 상태
  client: Client | null;
  isConnected: boolean;
  callbacks: ((answerAttemptId: string) => void)[];
  
  // 면접 진행 상태
  interviewInProgress: boolean;
  totalQuestions: number;
  completedQuestions: number;
  
  // 액션들
  connect, disconnect, startInterview, completeQuestion, endInterview;
}
```

#### 3. InterviewSetupStore (`src/shared/store/interviewSetupStore.ts`)
```tsx
// 면접 설정 관리
- 면접 타입, 질문 수, 업로드 파일 정보
- 단계별 진행 상태
- 폼 검증 상태
```

### 데이터 흐름 패턴

#### 1. 면접 설정 → 시작
```
InterviewSetup → InterviewGuide → InterviewLoading → InterviewSession
       ↓              ↓              ↓              ↓
   설정 저장     →   장비 확인    →   질문 생성    →   면접 진행
```

#### 2. 실시간 피드백 수집
```
InterviewSession → WebSocket → AI 분석 → Callback → UI 업데이트
       ↓              ↓           ↓         ↓         ↓
    답변 완료    →   데이터 전송  →  AI 처리  →  결과 수신  →  피드백 표시
```

#### 3. 세션 스토리지 활용
```tsx
// 질문 데이터 저장 키: REVIEW_SESSION_{interviewUuid}
{
  interviewUuid: string;
  interviewType: 'JOB' | 'TENACITY' | 'PT';
  questions: Question[];
  totalInterviewSets: number;
  // ... 기타 메타데이터
}
```

## 🛠️ 기술 스택 상세 분석

### 핵심 의존성
```json
{
  "react": "^19.1.0",                    // 최신 React (Concurrent Features)
  "typescript": "~5.8.3",               // 타입스크립트
  "vite": "^7.0.4",                     // 번들러/개발 서버
  
  // 상태 관리
  "zustand": "^5.0.6",                  // 경량 상태 관리
  "@tanstack/react-query": "^5.83.0",   // 서버 상태 관리
  
  // 화상 통신
  "openvidu-browser": "^2.30.0",        // 화상 회의
  
  // 실시간 통신
  "@stomp/stompjs": "^7.1.1",          // WebSocket (STOMP)
  "sockjs-client": "^1.6.1",           // SockJS
  
  // UI/UX
  "framer-motion": "^12.23.11",        // 애니메이션
  "tailwindcss": "3.4.1",              // CSS 프레임워크
  "lucide-react": "^0.534.0",          // 아이콘
  
  // 데이터 시각화
  "chart.js": "^4.4.0",                // 차트
  "react-chartjs-2": "^5.2.0",         // React Chart 래퍼
  
  // 기타
  "axios": "^1.11.0",                  // HTTP 클라이언트
  "react-router-dom": "^7.7.1",        // 라우팅
  "date-fns": "^4.1.0"                 // 날짜 처리
}
```

### 아키텍처 특징

#### 1. **모듈형 구조**
```
src/
├── pages/           # 페이지 컴포넌트
├── shared/          # 공통 모듈
│   ├── components/  # 재사용 컴포넌트
│   ├── hooks/       # 커스텀 훅
│   ├── store/       # 상태 관리
│   ├── api/         # API 클라이언트
│   └── types/       # 타입 정의
└── assets/          # 정적 자산
```

#### 2. **상태 관리 패턴**
- **Zustand**: 전역 상태 (인증, WebSocket)
- **React Query**: 서버 상태 캐싱
- **useReducer**: 복잡한 로컬 상태 (면접 세션)
- **SessionStorage**: 임시 데이터 저장

#### 3. **타입스크립트 활용**
- 엄격한 타입 검사
- 인터페이스 기반 설계
- 제네릭 활용한 재사용성

## 🚀 개선 사항 및 추천 수정 방향

### 🔥 즉시 개선 필요 사항

#### 1. **성능 최적화**
```tsx
// 현재 문제점: 과도한 리렌더링
const InterviewSession = () => {
  // 매 렌더링마다 새로운 객체 생성
  const handleAnalysisResult = (id: string) => { ... };
  
  useEffect(() => {
    connectWebSocket(handleAnalysisResult); // 의존성 배열에 함수
  }, [handleAnalysisResult]); // ❌ 매번 재연결
};

// 개선 방안: useCallback 활용
const handleAnalysisResult = useCallback((id: string) => {
  // 로직
}, [deps]); // ✅ 의존성 최적화
```

#### 2. **에러 처리 강화**
```tsx
// 현재: 기본적인 try-catch
try {
  const response = await apiCall();
} catch (error) {
  console.error(error); // ❌ 사용자 친화적이지 않음
}

// 개선: 구조화된 에러 처리
class APIError extends Error {
  constructor(public code: string, message: string) {
    super(message);
  }
}

const useErrorBoundary = () => {
  // 전역 에러 처리 로직
};
```

#### 3. **타입 안전성 개선**
```tsx
// 현재: any 타입 남용
const handleResponse = (response: any) => { // ❌
  return response.data?.result || response;
};

// 개선: 제네릭 활용
interface APIResponse<T> {
  data: {
    result: T;
    message: string;
  };
}

const handleResponse = <T>(response: APIResponse<T>): T => {
  return response.data.result;
};
```

### 📈 중장기 개선 방향

#### 1. **아키텍처 리팩토링**

**현재 구조의 문제점:**
- 컴포넌트 크기 과대 (InterviewSession.tsx: 831줄)
- 책임 분리 부족
- 상태 관리 분산

**개선 방안:**
```tsx
// 1. 컴포넌트 분리
InterviewSession/
├── InterviewSession.tsx          # 메인 컨테이너 (100줄 이하)
├── components/
│   ├── QuestionDisplay.tsx       # 질문 표시
│   ├── AnswerRecorder.tsx        # 답변 녹화
│   ├── ProgressTracker.tsx       # 진행률 추적
│   └── FeedbackCollector.tsx     # 피드백 수집
└── hooks/
    ├── useInterviewState.tsx     # 상태 관리
    ├── useRecording.tsx          # 녹화 로직
    └── useFeedbackCollection.tsx # 피드백 처리
```

#### 2. **상태 관리 최적화**
```tsx
// 현재: 분산된 상태 관리
const [state1, setState1] = useState();
const [state2, setState2] = useState();
const [state3, setState3] = useState();

// 개선: 통합 상태 관리
interface InterviewState {
  session: SessionState;
  recording: RecordingState;
  feedback: FeedbackState;
}

const useInterviewStore = create<InterviewState>((set) => ({
  // 통합된 상태 및 액션들
}));
```

#### 3. **코드 품질 개선**

**현재 문제점:**
- 매직 넘버 사용 (`60`, `5000`, `120000`)
- 하드코딩된 문자열
- 중복 코드

**개선 방안:**
```tsx
// constants/interview.ts
export const INTERVIEW_CONSTANTS = {
  ANSWER_TIME_SECONDS: 60,
  PREWAIT_TIME_MS: 5000,
  MAX_POLLING_TIME_MS: 120000,
  QUESTIONS_PER_SET: 3,
} as const;

// types/interview.ts
export type InterviewType = 'tech' | 'behavioral' | 'presentation';
export type InterviewStep = 'loading' | 'preparing' | 'answering' | 'complete';

// utils/interview.ts
export const createSessionKey = (uuid: string) => `REVIEW_SESSION_${uuid}`;
export const parseInterviewType = (raw: string): InterviewType => { ... };
```

#### 4. **테스트 코드 추가**
```tsx
// __tests__/InterviewSession.test.tsx
describe('InterviewSession', () => {
  it('should handle question progression correctly', () => {
    // 질문 진행 로직 테스트
  });
  
  it('should collect feedback properly', () => {
    // 피드백 수집 로직 테스트
  });
});

// __tests__/hooks/useWebSocket.test.ts
describe('useWebSocket', () => {
  it('should connect and receive messages', () => {
    // WebSocket 연결 테스트
  });
});
```

### 🔧 기술적 개선사항

#### 1. **번들 최적화**
```tsx
// vite.config.ts
export default defineConfig({
  build: {
    rollupOptions: {
      output: {
        manualChunks: {
          'react-vendor': ['react', 'react-dom'],
          'chart-vendor': ['chart.js', 'react-chartjs-2'],
          'openvidu-vendor': ['openvidu-browser'],
        }
      }
    }
  }
});
```

#### 2. **캐싱 전략 개선**
```tsx
// React Query 설정 최적화
const queryClient = new QueryClient({
  defaultOptions: {
    queries: {
      staleTime: 5 * 60 * 1000, // 5분
      cacheTime: 10 * 60 * 1000, // 10분
      refetchOnWindowFocus: false,
      retry: (failureCount, error) => {
        // 스마트 재시도 로직
        return failureCount < 3 && error.status >= 500;
      }
    }
  }
});
```

#### 3. **접근성 개선**
```tsx
// 키보드 네비게이션
const InterviewSession = () => {
  useEffect(() => {
    const handleKeyPress = (event: KeyboardEvent) => {
      if (event.key === ' ') { // 스페이스바
        event.preventDefault();
        handleNextQuestion();
      }
    };
    
    document.addEventListener('keydown', handleKeyPress);
    return () => document.removeEventListener('keydown', handleKeyPress);
  }, []);
  
  return (
    <div role="main" aria-label="면접 세션">
      <button 
        onClick={handleNextQuestion}
        aria-describedby="next-question-help"
      >
        다음 질문
      </button>
      <div id="next-question-help">
        스페이스바를 눌러 다음 질문으로 이동할 수 있습니다.
      </div>
    </div>
  );
};
```

## 📊 품질 메트릭스

### 현재 상태
- **코드 라인수**: ~15,000줄
- **컴포넌트 수**: 50개+
- **타입스크립트 커버리지**: 90%+
- **번들 크기**: 추정 2-3MB
- **성능 스코어**: 개선 필요 (큰 컴포넌트, 최적화 부족)

### 목표 지표
- **컴포넌트당 평균 라인수**: 100줄 이하
- **테스트 커버리지**: 80%+
- **번들 크기**: 1.5MB 이하
- **성능 스코어**: 90점 이상

## 🎯 우선순위별 개선 로드맵

### Phase 1: 즉시 개선 (1-2주)
1. **InterviewSession.tsx 분리** (831줄 → 100줄 이하)
2. **useCallback/useMemo 최적화**
3. **타입 안전성 개선** (any 타입 제거)
4. **에러 처리 표준화**

### Phase 2: 구조 개선 (3-4주)  
1. **커스텀 훅 분리**
2. **상태 관리 최적화**
3. **상수 및 유틸 정리**
4. **컴포넌트 재사용성 개선**

### Phase 3: 품질 향상 (5-8주)
1. **테스트 코드 작성**
2. **성능 최적화**
3. **접근성 개선**
4. **번들 최적화**

이 프로젝트는 전반적으로 탄탄한 기술 스택과 구조를 가지고 있지만, 컴포넌트 크기와 상태 관리 최적화, 테스트 코드 추가가 주요 개선 포인트입니다.